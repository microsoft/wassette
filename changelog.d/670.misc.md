Renamed each example's WIT `world` and `package` to match its published component name under the `microsoft` namespace (e.g. `package component:fetch-rs; world fetch;` is now `package microsoft:fetch-rs; world fetch-rs;`).
